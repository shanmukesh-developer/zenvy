import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.srm.hostelbitesadmin',
  appName: 'Admin Dashboard',
  webDir: 'out'
};

export default config;
